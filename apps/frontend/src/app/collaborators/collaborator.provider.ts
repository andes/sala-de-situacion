import { CollaboratorService } from './service/collaborator.service';
import { NgModule } from '@angular/core';

@NgModule({
    providers: [CollaboratorService]
})
export class CollaboratorProvidersModule { }
